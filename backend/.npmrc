# Force install devDependencies даже когда NODE_ENV=production
# (нужно для билда — tsc, tsx, drizzle-kit стоят в devDependencies).
# Runtime использует только prod deps через `node dist/server.js`.
production=false
